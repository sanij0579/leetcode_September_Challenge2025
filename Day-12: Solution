3227. Vowels Game in a String
Solution:
class Solution:
    def doesAliceWin(self, s: str) -> bool:
        vowels = set("aeiou")
        # Count vowels
        vowel_count = sum(1 for ch in s if ch in vowels)
        # If there are no vowels, Alice cannot play
        return vowel_count > 0
