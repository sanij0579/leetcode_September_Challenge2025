1733. Minimum Number of People to Teach
Solution:
class Solution:
    def minimumTeachings(self, total_langs, users_langs, friends):
        teach_users = set()

        # Step 1: Identify users who can't communicate
        for u1, u2 in friends:
            u1 -= 1  # Convert to 0-based index
            u2 -= 1
            can_talk = False

            for l1 in users_langs[u1]:
                if l1 in users_langs[u2]:
                    can_talk = True
                    break

            if not can_talk:
                teach_users.add(u1)
                teach_users.add(u2)

        # Step 2: Find minimum users to teach for any language
        min_teach = len(users_langs) + 1

        for lang in range(1, total_langs + 1):
            cnt = 0
            for u in teach_users:
                if lang not in users_langs[u]:
                    cnt += 1
            min_teach = min(min_teach, cnt)

        return min_teach
